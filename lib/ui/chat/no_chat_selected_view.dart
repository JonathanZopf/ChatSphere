import 'package:flutter/material.dart';

class NoChatSelectedView extends StatelessWidget {
  const NoChatSelectedView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('No chat selected'),
    );
  }
}